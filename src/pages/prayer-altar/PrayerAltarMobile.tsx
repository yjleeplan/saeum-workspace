import { useQuery } from '@tanstack/react-query';
import { useEffect, useState, useMemo } from 'react';
import { queries } from 'api/queries';

const PrayerAltarMobile = () => {
  return <></>;
};

export default PrayerAltarMobile;
